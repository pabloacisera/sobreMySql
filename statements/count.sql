SELECT count(age) FROM profesionales_mysql.users;/*aqui se cuentan todos los usuarios que tengan 
expresada la columna edad, por ejemplo si tenemos 20 usuarios y todos completaron el campo edad, 
un resultado de 20*/

si queremos contar el valor total de sumar todas las edades tenemos SUM(age);

